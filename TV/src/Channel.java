//channel class to change the channels

public class Channel {
	boolean prevchannel;
	boolean nextchannel;

	public void channel() {
		if (prevchannel == true) {
			// if previous button is pressed then go to previous channel
		}

		if (nextchannel == true) {
			// if next button is pressed then go to next channel
		}
		// if numbers are pressed between 0-9 then jump to that channel

	}

}
